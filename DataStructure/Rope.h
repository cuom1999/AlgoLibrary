#include <ext/rope> //header with rope
using namespace __gnu_cxx; //namespace with rope and some additional stuff

// constructor: rope<int> r 
// push_back()
// insert(pos, val)
// substr(pos, len)
// delele(pos, len)
// replace(pos, len, val)
// += rope
// rope + rope